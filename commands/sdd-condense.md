---
description: SPEC.md condenser — token-budget sweep. Use when user asks to condense spec or check skill emits token-budget overflow advisory ("condense SPEC.md", "SPEC too big", "shrink the spec", "token budget").
---

invoke the condense skill: $ARGUMENTS
